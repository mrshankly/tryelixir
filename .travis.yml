language: erlang
otp_release:
    - 17.1
before_install:
    - git clone --branch=v1.0.1 http://github.com/elixir-lang/elixir
    - cd elixir && make && cd ..
before_script: "export PATH=`pwd`/elixir/bin:$PATH"
script: "MIX_ENV=test mix do deps.get, test"
